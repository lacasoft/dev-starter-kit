---
description: Muestra el estado del proyecto y del kit (memoria, stack, sesión).
---

Reporta de forma concisa:
1. Stack detectado (lee `./CLAUDE.md` y el bloque dev-starter-kit).
2. Entradas en la memoria del proyecto (`.claude/memory/memory.jsonl`) y última sesión (`session.json`).
3. Si claude-flow está activo (`.mcp.json` con server `claude-flow`).
4. Agentes y skills disponibles en `.claude/agents/` y `.claude/skills/`.
